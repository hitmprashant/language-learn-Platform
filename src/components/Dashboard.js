// components/Dashboard.js

import React from 'react';

const Dashboard = () => {
  return (
    <div>
      <h2>Dashboard</h2>
      {/* Display user dashboard with progress, recent lessons, etc. */}
    </div>
  );
}

export default Dashboard;
